---
description: Create, resync, or restructure the repo's ARCHITECTURE.md (constitution + map). Out-of-run entry point.
argument-hint: '[create | resync | restructure]'
disable-model-invocation: true
---

Load and follow the ThisIsEnough **tie:map** skill for this repository: create, resync, or restructure `ARCHITECTURE.md` (constitution + map). This is the manual, out-of-run entry point; during runs the orchestrator updates the map automatically.

Treat everything below as the user's intent for this invocation (it may be empty):

$ARGUMENTS
